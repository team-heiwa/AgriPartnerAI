# Terraform
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/terraform.tfvars
!terraform/terraform.tfvars.example

# Terraform lock file
terraform/.terraform.lock.hcl

# General Terraform artifacts (root of infra or any subdirectory)
.terraform/
*.tfstate
*.tfstate.*
*.tfplan

# Local variable files
*.auto.tfvars
terraform.tfvars
!terraform.tfvars.example

# Service account keys (anywhere un
der infra)
*-key.json
service-account-key.json